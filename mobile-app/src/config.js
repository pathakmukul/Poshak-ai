// API Configuration
// For React Native, we need to use the actual IP address instead of localhost
// Change this based on your development setup:
// - Android Emulator: use 10.0.2.2
// - iOS Simulator: use localhost or your machine's IP
// - Physical device: use your machine's IP address

const API_URL = 'http://10.0.2.2:5001'; // For Android emulator
// const API_URL = 'http://localhost:5001'; // For iOS simulator
// const API_URL = 'http://192.168.x.x:5001'; // Replace with your machine's IP for physical device

export default API_URL;